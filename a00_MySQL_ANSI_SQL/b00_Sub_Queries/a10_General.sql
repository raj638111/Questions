
-- Subquery

  + Can return a
    - Scalar (Single value) (Scalar sub query)
    - Single row (Row sub query)
    - Single column (Column query)
    - Table (ie N rows * M columns) (Table sub query)

  + Can occur in,
    - select
    - from
    - where
    - having